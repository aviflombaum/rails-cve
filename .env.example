# Copy to .dev.vars for local development. Generate separate production secrets.
ENCRYPTION_KEY=replace-with-64-random-hex-characters
ADMIN_TOKEN=replace-with-64-random-hex-characters
# Optional, for higher GitHub API rate limits:
# GITHUB_TOKEN=
