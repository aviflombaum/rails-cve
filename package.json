{
  "name": "rails-cve",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --config wrangler.deploy.jsonc",
    "typecheck": "wrangler types && tsc --noEmit",
    "test": "vitest run",
    "db:local": "wrangler d1 migrations apply DB --local",
    "db:remote": "wrangler d1 migrations apply DB --remote --config wrangler.deploy.jsonc",
    "format": "prettier --write src scripts test/*.ts public/style.css public/app.js egress test/*.mjs",
    "setup": "bun scripts/setup.ts",
    "check": "bun run format:check && bun run typecheck && bun run test && bun run test:egress",
    "format:check": "prettier --check src scripts test/*.ts public/style.css public/app.js egress test/*.mjs",
    "deploy:cloudflare": "bun scripts/deploy-cloudflare.ts",
    "test:egress": "node --test test/egress.test.mjs"
  },
  "dependencies": {
    "hono": "^4.13.2",
    "ipaddr.js": "^2.2.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@types/node": "^26.6.2",
    "prettier": "^3.9.8",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "wrangler": "^4.123.0"
  },
  "version": "0.1.0",
  "description": "Rails security advisories delivered as signed webhooks and agent-ready investigation briefs.",
  "license": "MIT",
  "author": "Avi Flombaum",
  "engines": {
    "node": ">=22",
    "bun": ">=1.3.14"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/aviflombaum/rails-cve.git"
  },
  "homepage": "https://rails-cve.avi.nyc",
  "bugs": {
    "url": "https://github.com/aviflombaum/rails-cve/issues"
  },
  "overrides": {
    "sharp": "0.35.4"
  }
}
